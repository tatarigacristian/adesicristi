"use client";

// This page is handled by the layout:
// - If not authenticated, the layout shows the login form.
// - If authenticated, the layout redirects to /admin/guests.
// This component renders nothing (the layout handles everything).
export default function AdminPage() {
  return null;
}
